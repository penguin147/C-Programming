#include <stdio.h> 

int main(void)
{
	printf("이름: 홍길동\n");//괄호 안에 있는 것을 출력해 주는 printf함수를 이용해 문자열인‘주소: 서울시 종로구’을 출력하고 \n(줄바꿈문자)를 이용해 줄을 바꾼다.
	printf("주소: 서울시 종로구\n");//괄호 안에 있는 것을 출력해 주는 printf함수를 이용해 문자열인‘전화번호: 111-2222’를 출력하고 \n(줄바꿈문자)를 이용해 줄을 바꾼다.
	printf("전화번호: 111-2222\n");//main함수에서 0값을 반환한다.
	return 0;//main함수에서 0값을 반환한다.
}
